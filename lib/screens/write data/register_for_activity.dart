import 'package:flutter/material.dart';

class RegisterForActivity extends StatelessWidget {
  const RegisterForActivity({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
